import React from "react";

export const Profile = (userId) => {
  // Fetch user data based on the userId or perform any other necessary logic

  return (
    <div>
      <h1>User Profile</h1>
      <p>User ID: {userId}</p>
      {/* Additional user profile information */}
    </div>
  );
}
